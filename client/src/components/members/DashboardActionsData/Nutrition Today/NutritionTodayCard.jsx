import React from "react";

const NutritionTodayCard = ({open, setOpen}) => {
    if (!open) return null
  return (
    <div className="fixed inset-0 flex items-center justify-center z-50 bg-black bg-opacity-40">

    </div>
  );
};

export default NutritionTodayCard;
